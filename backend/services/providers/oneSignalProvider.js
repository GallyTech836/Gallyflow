import { oneSignalConfig } from '../../config/onesignal.js';

export async function send(oneSignalId, mensaje) {
  // TODO (Fase 2): fetch real a oneSignalConfig.apiUrl
  throw new Error('[oneSignalProvider] Envío real no implementado todavía (Fase 2).');
}